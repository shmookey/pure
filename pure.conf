# Sample configuration file

server {
  port     = 3000
  pidFile  = "/var/run/pure/pure.pid"
  logFile  = "/var/log/pure.log"
  logLevel = "info"
}

repos {
  path = "/var/run/pure/repos"
}

keys {
  path = "/var/run/pure/keys"
}

